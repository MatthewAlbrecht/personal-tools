/**
 * Production Next.js origin (no trailing slash). All ingest POSTs go here so Convex uses your
 * deployed env (prod). If your Vercel URL changes, update this assignment and host_permissions in
 * manifest.json for the same hostname.
 */
globalThis.RYM_INGEST_ORIGIN = "https://personal-tools-phi.vercel.app";
